let express,mainRouter;_f14‍.w("express",[["default",["express"],function(v){express=v}]]);_f14‍.w("./routes/main.js",[["default",["mainRouter"],function(v){mainRouter=v}]]);

const app = express();
const port = 3000;





app.use(express.json());
app.use(express.urlencoded({ extended: false }));

app.use("", mainRouter);
// app.use("/user", userRouter);


app.get("/health", (req, res) => {
  res.send("I'M ALIVE");
});

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`);
});